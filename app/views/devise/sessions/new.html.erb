<div class="text-center">
  <h2>Log in</h2>
</div>

<div class='text-center'>
  <p>Don't have account yet?<br><%= link_to("It's easy, create one here", new_user_registration_path ) %></p>
</div>

<div class="login-form">
  <div class="socialicon">
  <img src="/assets/facebook-circle-icon-ea5922b8b44669d53b4517dd356d81c36658b2d2008b0e2ba2916d3948aa6d4b.png" alt="Facebook circle icon">
   
    <%= link_to "Log in with Facebook" %>
    <%= image_tag 'google-circle-icon' %>
    <%= link_to "Log in with Google" %>
  </div>
</div>

<%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
  
  <div class="setup-wrapper" >
    <%= bootstrap_form_for(@user) do |f| %>
    <div class='setup-form'>
      <%= f.email_field :email %>
      <%= f.password_field :password %>
      <div class="center-text">
        <%= link_to "Forgot your password?", new_password_path(resource_name) %><br />
      </div>
      <br>
      <%= f.check_box :remember_me %>
      <%= f.submit "Log in", class: "btn btn-link setup-button" %>
    </div>
    <% end %>
  </div>

<% end %>

<% render "devise/shared/links" %>
