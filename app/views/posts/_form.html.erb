<%= form_with(model: post, local: true) do |form| %>
  <% if post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_area :title, id: :post_title %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :post_description %>
  </div>

  <div class="field">
    <%= form.file_field :images, multiple: true %>
  </div>

  <div class="field">
    <%= form.label :shipping %>
    <%= form.check_box :shipping, id: :post_shipping %>
  </div>

    <div class="field">
    <%= form.label :brand %>
    <%= form.text_area :brand, id: :post_brand %>
  </div>

    <div class="field">
    <%= form.label :size %>
    <%= form.text_area :size, id: :post_size %>
  </div>

      <div class="field">
    <%= form.label :color %>
    <%= form.text_area :color, id: :post_color %>
  </div>

      <div class="field">
    <%= form.label :category %>
    <%= form.text_area :category, id: :post_category %>
  </div>

  <div class="field">
    <%= form.label :condition %>
    <%= form.text_area :condition, id: :post_condition %>
  </div>

  <div class="field">
    <%= form.label :asking_price %>
    <%= form.text_field :asking_price, id: :post_asking_price %>
  </div>

  <div class="field">
    <%= form.label :auction %>
    <%= form.check_box :auction, id: :post_auction %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
